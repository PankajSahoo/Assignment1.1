1.  v <- c( 2,5.5,6)
    t <- c(8, 3, 4)
    print(v%/%t)

Ans:> it produces results:->   0  1  1


3. If the above 25 files were csv files, what would be your script to read?
Ans:>  We will use the built in {read.csv(...)} function call, which reads the data .
